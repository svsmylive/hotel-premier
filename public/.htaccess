<IfModule mod_rewrite.c>
    <IfModule mod_negotiation.c>
        Options -MultiViews -Indexes
    </IfModule>

    RewriteEngine On

    RewriteRule ^nomernoy-fond-2$ / [R=301,L]
    RewriteRule ^nomernoy-fond-2/$ / [R=301,L]

    RewriteRule ^nomernoy-fond-2/nomer-klassa-standart$ / [R=301,L]
    RewriteRule ^nomernoy-fond-2/nomer-klassa-standart/$ / [R=301,L]

    RewriteRule ^sample-page/park-krasnodar$ / [R=301,L]
    RewriteRule ^sample-page/park-krasnodar/$ / [R=301,L]

    RewriteRule ^restorannyiy-kompleks$ / [R=301,L]
    RewriteRule ^restorannyiy-kompleks/$ / [R=301,L]

    RewriteRule ^dlya-sporta-i-otdyiha$ / [R=301,L]
    RewriteRule ^dlya-sporta-i-otdyiha/$ / [R=301,L]

    RewriteRule ^konferents-zal$ / [R=301,L]
    RewriteRule ^konferents-zal/$ / [R=301,L]

    RewriteRule ^raspolozhenie$ / [R=301,L]
    RewriteRule ^raspolozhenie/$ / [R=301,L]

    RewriteRule ^kontaktyi$ / [R=301,L]
    RewriteRule ^kontaktyi/$ / [R=301,L]


    # Handle Authorization Header
    RewriteCond %{HTTP:Authorization} .
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

    # Redirect Trailing Slashes If Not A Folder...
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_URI} (.+)/$
    RewriteRule ^ %1 [L,R=301]

    # Send Requests To Front Controller...
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>
